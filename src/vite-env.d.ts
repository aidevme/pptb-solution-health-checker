/**
 * Vite client type declarations — do not delete or move this file.
 *
 * @remarks
 * The triple-slash directive pulls in Vite's `vite/client` type augmentations,
 * which provide TypeScript support for:
 * - `import.meta.env` (`MODE`, `BASE_URL`, `DEV`, `PROD`, and custom `VITE_*` variables)
 * - `import.meta.hot` (HMR API)
 * - Static asset imports (`.svg`, `.png`, `.css?inline`, etc.)
 * - CSS Modules (`*.module.css`)
 *
 * This file must remain inside `src/` so TypeScript picks it up as part of the
 * compilation root. Moving it outside `src/` silently drops all Vite-specific
 * type augmentations without a compile error.
 *
 * @packageDocumentation
 */
/// <reference types="vite/client" />
